services:
  mongo:
    image: mongo:latest
    container_name: calendar-mongo
    environment:
      MONGO_INITDB_ROOT_USERNAME: mern_user
      MONGO_INITDB_ROOT_PASSWORD: "1234567"
    ports:
      - "27017:27017"
    volumes:
      - mongo_data:/data/db

volumes:
  mongo_data:
